# The base URL of the RAG API backend.
# IMPORTANT: The backend's .env MUST have SECURITY_ALLOWED_ORIGINS configured
# to include this frontend's origin (e.g., http://localhost:3000) for CORS to work.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
